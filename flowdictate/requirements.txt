# FlowDictate — macOS AI Voice Dictation App
# Python 3.12+ required | Apple Silicon optimised

# Audio capture
sounddevice==0.5.1
numpy==2.2.6

# Transcription — Cloud
openai==1.86.0

# Transcription — Local (offline / privacy mode)
faster-whisper==1.1.1

# LLM cleanup
anthropic==0.55.0

# Global hotkey listener
pynput==1.8.1

# Clipboard & text injection
pyperclip==1.9.0

# macOS menu bar
rumps==0.4.0

# macOS system integration (pyobjc)
pyobjc-core==11.0
pyobjc-framework-Cocoa==11.0
pyobjc-framework-AppKit==11.0
pyobjc-framework-Quartz==11.0

# Environment / config
python-dotenv==1.1.0

# tkinter is bundled with Python — no pip install needed
